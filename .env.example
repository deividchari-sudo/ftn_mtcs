# ============================================================================
# Configuração de Variáveis de Ambiente - Fitness Metrics Dashboard
# ============================================================================
# IMPORTANTE: Este arquivo é um TEMPLATE. Copie para .env e preencha seus valores.
# NUNCA faça commit de .env no repositório (já está em .gitignore)
# ============================================================================

# API Groq para IA especialista em triathlon
# Gerar em: https://console.groq.com/
# Formato esperado: ~40 caracteres alfanuméricos
GROQ_API_KEY=sua_chave_api_groq_aqui

# Ambiente de execução (development ou production)
# Em produção, o .env NÃO será carregado automaticamente
ENV=development

# ============================================================================
# SEGURANÇA
# ============================================================================
# As credenciais do Garmin são ENCRIPTADAS automaticamente:
# - Salvas em: ~/.fitness_metrics/garmin_credentials.json
# - Senha: criptografia AES-128 (Fernet)
# - Chave: derivada de machine-id + PBKDF2
# - Permissões: 0o600 (apenas owner lê)
#
# Os tokens OAuth são PROTEGIDOS:
# - Salvos em: ./garmin_tokens.json/
# - Permissões: 0o700 para diretório (apenas owner)
# - Permissões: 0o600 para arquivos (apenas owner)
# - Validação automática de expiração
# - Renovação automática quando necessário
# ============================================================================

# ============================================================================
# COMO USAR ESTE ARQUIVO
# ============================================================================
# 1. Copie este arquivo:
#    cp .env.example .env
#
# 2. Edite .env e preencha seus valores:
#    nano .env  (Linux/Mac)
#    code .env  (VS Code)
#
# 3. NUNCA commite .env no git:
#    git status # verificar que .env não aparece
#
# 4. Para máquinas de produção, use variáveis de ambiente do sistema:
#    export GROQ_API_KEY='sua_chave'
#    python app.py
# ============================================================================